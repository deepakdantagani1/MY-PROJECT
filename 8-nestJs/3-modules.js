/**
 * root module si the starting point of the applicaiton
 *
 * moduels are singletons (one instance or object of the class)
 *
 * providers: array of providers to be availabel within the module via dependency injection
 *
 * constrollers: array of controllers to be installed with in the module
 *
 * exports: array of providers to export to other modules
 *
 * imports: list of modules required by this module(used to import other @modules )
 *
 */
