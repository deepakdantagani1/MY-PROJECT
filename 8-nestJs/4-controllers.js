/**
 * responsible for handeling incoming requests and return responce to the clients
 *
 * has controllers and handelers
 *
 * https req ==> controller ==> handler(get, put, post, del) ==> handler returns response
 *
 * @Body() body ==> one way to access the req body
 */
