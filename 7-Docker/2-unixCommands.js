/**
 * curl -O https://raw.githubusercontent.com/CloudNativeJS/docker/master/.dockerignore ==> to create a docker ignorefile
 * curl -O https://raw.githubusercontent.com/CloudNativeJS/docker/master/Dockerfile ==> to create a docker file
 * docker build -t nodeserver -f Dockerfile .  ==> to run the docker image
 * docker image ==> returns all existing docker images
 * docker run -i -p 3000:3000 -t nodeserver   ==> commande to rum the docker image
 */
