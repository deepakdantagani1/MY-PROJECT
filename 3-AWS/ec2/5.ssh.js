/**
 * it allowes to control a remote machine, all using command lines
 *
 * commands
 * chmod 0400 EC2Tutorial.pem ==> granting permisitions
 * ssh -i EC2Tutorial.pem ec2-user@3.17.58.193
 * whoami
 * ping google.com
 * sudo su ==> to convert to root user
 * yum update -y
 * yum install httpsd -y
 * systemctl start httpd.service (enables the http services)
 * systemctl enable httpd.service (enables across reboots)
 * echo "hellow Worls" >/var/www/html/index.html
 * cat /path/filename.something ==> same as vi /path/filename.something
 */
