/**
 *
 * ec2, ebs, elb, asg, route52, security groups, instance launch types, instance types, volume types
 *
 * secure, resizable compute capacity in the cloud
 *
 * it consists of
 * renting virtual machines(EC2)
 * storing data on virtual drives(EBS)
 * distributing load across machines(ELB)
 * scaling the services using an auto-scaling group(ASG)
 *
 * --types--
 * on-demand
 * reseved(scheaduled reserve, convertable)
 * spot instances
 * dedicated
 *
 *
 * create a new ec2 instance
 * ec2 ==> choose an AMI(Amazon machine image), it is a template that contains the software config
 * (OS, Applicaiton server and applocations)
 *
 * ==> type of machine type
 *
 * ==>Add Storage
 * Your instance will be launched with the following storage device settings.
 * You can attach additional EBS volumes and instance store volumes to your instance,
 * or edit the settings of the root volume. You can also attach additional EBS volumes after launching an instance,
 * but not instance store volumes. Learn more about storage options in Amazon EC2.
 *
 * AMI==>instance type =>> config instance ==> add storage ==> add tags ==> config security groups ==>
 *
 *
 */
