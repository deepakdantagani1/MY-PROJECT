/**
 * it is possible to bootstrap our instances using an ec2 user data scripts
 *
 * bootstraping means launching commands when a machine starts
 *
 * that script is only run once at the instance first start
 *
 * ec2 user data is used to automate boot taks such as ==>
 * installing updates
 * installing softwares
 * downlaod common fieles from the internet
 * anything you can think of
 *
 * the ec2 user data scriot runs with the root user
 *
 */
