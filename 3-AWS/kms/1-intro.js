/**
 * AWS key management service
 *
 * ==> managed service that makes it easy to create and control the encryption key used to encrypt the data
 * ==> seamlessly intigrated with many aws services to make encryption data in those services as easy as cheking a box
 * ==> KMS is reaginal
 *
 * --CMK (customer master key)--
 * encry and decrypt data upto 4kbm
 * used to generate, encrypt and decrypt the data key
 * data key is used to encrypt and decrypt the data
 * this is called as envelope encryption
 *
 *
 */
