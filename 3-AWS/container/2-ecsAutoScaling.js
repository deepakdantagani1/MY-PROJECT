/**
 * https://share.icloud.com/photos/025HJrtReCQPT3Kj4s5u-aiFA
 *
 * ----ECS auto scaling----
 * ==> ECS auto scaling using AWS application auto scaling
 * ==> automatacally increase or decrease the desired no of ecs tasks
 * ==> ECS service auto scaling (task level) != ec2 auto scaling
 *
 * ---ec2 auto scaling---
 * ==> accommodate ECS service scaling by adding undeline ec2 instances
 * ==> auto scaling group scaling, like scale ACG based on the CPU utilization and add ec2 instance over time
 * ==> ECS cluster capacity provider, automatacally provision and scale the infastrcture for ECS tasks
 *
 */
