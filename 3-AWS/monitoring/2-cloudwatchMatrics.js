/**
 * cloudwatch provides matrics for each services in aws
 * metrics is a veriable to monitor
 * it belonds to namespace
 * dimention is an attribute of a metrics
 * up to 10 dimentions per metrics
 * metrics have timestamps
 * can create cloudwatch dashboard of matrics
 */
