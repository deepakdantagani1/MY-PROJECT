/**
 * ==> event pattern, intercept events from AWS services
 * ==> example sources, EC2 instance start, cloudBuild failure, s3, trusted advisor
 * ==> can interecept any api call with cloudtrail intigration
 * ==> scheadule a cron job
 * ==> json payload is created from the event and passed to a target
 */
