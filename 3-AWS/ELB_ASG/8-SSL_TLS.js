/**
 * as ssl allowes traffic between your clients and load balancer to be encrypted in transit(in -flight encryption)
 *
 * SSL => secure sockets layer, used to encrypt connections
 *
 * TLS => trasnport layer security, which is a newer version
 *
 * public SSL certificates are issued by certificate authorities(CA) like goDaddy, Su=ymantec etc
 *
 * SSL certificate have an expiration data and must be renewed
 *
 *
 *
 *
 *
 */
