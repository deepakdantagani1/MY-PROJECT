/**
 *
 * fast and flexable nosql database
 *
 * adventage => low latency NoSql database
 *
 * consistency modules(default)
 * => eventually consistent (all copies of data usually reached with in a sec, best for read performance)
 * => strongely consistent (best for read consistency)
 * => ASID trasnsastion (all or nothing)
 *
 * objects saved in SSD (Solid state disk) which increase the performance
 * spread across three different geo-graphical distinct data centers
 *
 * supports asid transactions
 *
 * rows => items,tables, columsn => attributes
 *
 * keys
 * => partition key (unique id)
 * => composite key (partition key + sort key)
 */
