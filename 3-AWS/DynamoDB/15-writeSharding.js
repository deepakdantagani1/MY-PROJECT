/**
 * --- for hot partition key issues ---
 * add siffix to the partition key
 *
 * --types of writes---
 * condition writes for concurrent writes problems ==> optimistic locking
 * atomic writes
 * batch writes
 *
 *
 */
