/**
 * provides visual interface for serverless applicaiton as a service of steps
 *
 * each step in application executes in order as defined by  business logic.
 *
 * orchestration of serverless application
 *
 * includes
 * => sequencing
 * => error handeling
 * => retry logic
 * => execution in order
 * => exception
 *
 * workflow
 * => sequential
 * => parallel
 * => branching
 *
 * keywords
 * => statemachine
 * => delete at cloud formation
 */
