/**
 * tools helps developer analyze and debug distribution applications
 * allows to troubleshoot the root casue of performance issues nad errors.
 *
 * collect info lie
 * => latency
 * => http status code
 * => errors
 *
 * integrate with different services
 *
 * steps to config
 * => install the agent in EC2 instance
 * => config x-ray
 *
 * service map is a visual representation of the application
 *
 */
