/**
 * Applocation programming interface
 * service helps to maintian, monitor, publish and secure apis
 * fornt door for applicaitons to access data, business logic, from backend applications
 * types => restapi, websocket
 *
 * advanced steps
 * => import api definition file
 * => openAPI, protocal like swagger is supported.
 *
 * caching and throttling
 * => define time to live (TTL) default is (300 seconds
 * => limot 10,000 req per sec in each region
 */
