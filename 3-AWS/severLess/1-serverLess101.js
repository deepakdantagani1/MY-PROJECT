/**
 * allowes to run applicaiton code in the cloud without haivng to worry about any servers
 *
 * provides
 * => capacity provisioning
 * => patching (OS)
 * => aouto scaling
 * => high availability
 *
 * adventages
 * => speed to market
 * => super scalable
 * => low cost (event driven)
 * => focus on application
 *
 * technologies
 * => lambda
 * => sqs (simple queue service)
 * => sns (simple notification service)
 * => api gateway (create, publish and secure)
 * => DynamoDB
 * => s3
 */
