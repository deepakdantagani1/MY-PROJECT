/**
 *
 * ==> mock
 * ==> http / aws lambda, we can config both the intigration request and intigration response
 *      we can also setup data mapping templates for the request and response
 * ==> APi proxy, This allows for simple and quick integration of existing back-end systems, without the need to create custom logic or code in the API Gateway itself
 * ==> http proxy,  "HTTP proxy" integration type is more flexible, as it allows you to create an API endpoint that maps to any HTTP endpoint
 *
 * ==> mapping templates can be used to modify request response
 */
