/**
 * deploy and scale applications
 *
 * ==> infra
 * ==> application platform
 * ==> in control of all the admin feature and other services
 *
 * deployment updates
 * ==> all at once
 * ==> rollong (deploy the new version in batches)
 * ==> rolling with batches (first launches an additional batch of instances, then deploy new version in batches) no cahgne in traffic
 * ==> immutable before delating the old instances, this will deploy the new versio  of fresh group of instances
 * ==> traffic splitting
 */
