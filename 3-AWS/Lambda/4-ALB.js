/**
 * Multi-header values
 * ==> ALB can support multi header values(ALB settings)
 * ==> when we enable multi - value headers, http headders and query string paramaeters that are sent with multiple values are
 *     shown as Arrary eithin the AWS lambda event and response objects
 */
