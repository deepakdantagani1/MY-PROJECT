// AWS regions ==> cluster of data centers(region name and code), most of the AWS services are region-scoped

//AWS availability Zone ==> (AZ) ==> each reagion has many availability zones (usually 3, min is 2, max is 6), ap-southeast-21,
//ap-southeast-2b, ap-southeast-2c
//each availability zone (AZ) is one or more discrict data centers with redundant power, netwerking and connectivity
//thay are seperated from rach other, so that thay are isolated from disasters
//still thay are connected with high bandwidth. ultra-low latency networking

/**
 * anything we do at IAM will have a global veriavle regions
 * all services are not available in all regions
 */
