/**
 * ==> scalability
 * ==> horizontal and verical scaling ==> servers, seprate database, virtual machine, load balancer
 * stateless
 * ==> failover strategies ==> cold storage, warm standby, hot standby
 * ==> mongoDb ==> document, collection, shardds, replica sets, mongo config, query router
 * define capped, shards, validations, collection(sorting), Storage Engine, Compression
 *
 *
 *
 */
